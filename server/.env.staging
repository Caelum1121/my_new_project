NODE_ENV=staging
PORT=3000

# API URL (pointing to staging host)
API_URL=http://3.86.27.241:3000/api

# Database connection
# (service name "mongo" works inside docker-compose; DB name can be different if you want to keep staging separate)
MONGO_URI=mongodb://mongo:27017/rmit_ecommerce_staging

# JWT secret (use a different one from production for safety)
JWT_SECRET=some_staging_secret

# Frontend client URL (staging server’s public IP)
CLIENT_URL=http://3.86.27.241:8080

# Base API prefix
BASE_API_URL=api