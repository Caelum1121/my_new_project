NODE_ENV=production
PORT=3000

# API URL (pointing to production host)
API_URL=http://3.89.222.158:3000/api

# Database connection
MONGO_URI=mongodb://mongo:27017/rmit_ecommerce_prod

# JWT secret (use a strong, unique random value here)
JWT_SECRET=some_production_secret_CHANGE_ME

# Frontend client URL (production server’s public IP or your domain)
CLIENT_URL=http://3.89.222.158:8080
# (replace with https://yourdomain.com when you have a domain + SSL)

# Base API prefix
BASE_API_URL=api
